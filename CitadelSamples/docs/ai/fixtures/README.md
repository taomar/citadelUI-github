# SIMULATION fixtures: fresh start and recovery

**SIMULATION ONLY. Not passed rehearsals, live tasks, real provider records or
authority to act.** All identifiers below are synthetic except the project
guidance paths. No credentials, launch fragments, endpoints or cloud IDs are used.
Do not execute commands or mutate files/resources on behalf of these fixtures.

Inputs: [scenarios.json](scenarios.json) and the actual saved
[unfinished artifact](unfinished-artifact.md). The parent launches the NEW
read-only native session with the [rehearsal prompt](../STARTUP_PROMPTS.md#fresh-session-rehearsal).
Record its observed answers and loading evidence separately in a versioned result
packet bound to the integrated candidate. This README is a rubric, not evidence
that the scenarios ran.

| Scenario | Evidence the rehearsal must produce |
| --- | --- |
| Fresh resume | Locate and quote the actual partial artifact, distinguish completed draft from unresolved question, retain budget, identify smallest read-only continuation and need for writer transfer |
| Stale result | Compare stable task ID, assignment v2, owner and contract against v1 result; reject integration even though v1 says its check passed; preserve its bytes as reference only |
| Unknown effect | Do not retry the timed-out operation; use the supplied simulated operation/resource readback, resolve succeeded from that evidence, and record no live call was made |
| Route classification/change | Classify the label fix as ordinary child implementation; classify shared relay state/scale-out as material architecture work; stop affected v1 assignments, preserve partials, obtain authority/version new contract and reassign; unaffected copy task may continue |

An unanswered product question must stay unanswered until supported evidence or
owner decision resolves it. The existence of a README expected answer does not
make automatic instruction loading, worker interruption, live deployment or
unattended coordinator recovery verified.
